/**
 * ExecuteGetEvaluationRun - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: evaluations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetEvaluationRun Use Case
 *
 * DDD: Application use case for get Evaluation.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetEvaluationRunInput, GetEvaluationRunOutput } from "../dto/evaluation.dto";
import type { ExecutionContextService, IdGeneratorService } from "@modelescrow/services/_shared/index.js";
import type { EvaluationRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetEvaluationRun {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly evaluation: EvaluationRepository
  ) {}

  async execute(input: GetEvaluationRunInput): Promise<GetEvaluationRunOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.evlId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).evaluationRunId) {
      throw new ValidationError("evaluationRunId is required");
    }
    const evaluationrunId = (input as any).evaluationRunId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.evaluation.getEvaluationRun({ evaluationRunId: evaluationrunId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${evaluationrunId}`);
    }

    return entity as GetEvaluationRunOutput;
  }
}
