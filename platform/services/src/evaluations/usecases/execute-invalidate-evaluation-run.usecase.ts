/**
 * ExecuteInvalidateEvaluationRun - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: evaluations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteInvalidateEvaluationRun Use Case
 *
 * DDD: Application use case for get Invalidate.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { InvalidateEvaluationRunInput, InvalidateEvaluationRunOutput } from "../dto/invalidate.dto";
import type { ExecutionContextService, IdGeneratorService } from "@modelescrow/services/_shared/index.js";
import type { InvalidateRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteInvalidateEvaluationRun {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly invalidate: InvalidateRepository
  ) {}

  async execute(input: InvalidateEvaluationRunInput): Promise<InvalidateEvaluationRunOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.evlId();

    // All field names extracted from OpenAPI spec: reason
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).evaluationRunId) {
      throw new ValidationError("evaluationRunId is required");
    }
    const evaluationrunId = (input as any).evaluationRunId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.invalidate.invalidateEvaluationRun({ evaluationRunId: evaluationrunId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${evaluationrunId}`);
    }

    return entity as InvalidateEvaluationRunOutput;
  }
}
