/**
 * ExecuteListSubmissions - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: submissions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListSubmissions Use Case
 *
 * DDD: Application use case for list Submission.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListSubmissionsInput, ListSubmissionsOutput } from "../dto/submission.dto";
import type { ExecutionContextService, IdGeneratorService } from "@modelescrow/services/_shared/index.js";
import type { SubmissionRepository } from "../ports";

export class ExecuteListSubmissions {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly submission: SubmissionRepository
  ) {}

  async execute(input: ListSubmissionsInput): Promise<ListSubmissionsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.subId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.submission.listSubmissions({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListSubmissionsOutput;
  }
}
